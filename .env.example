# Example environment configuration for local development
# Copy this file to .env and run `direnv allow` to use it

# Database connection string
export DATABASE_URL="postgres://postgres:postgres@localhost:5432/ezlime"

# API keys (comma-separated list)
# These are used for authenticated endpoints like /link/create
export KEYS="test-key-1,test-key-2,dev-key"

# URL prefix for shortened links
export URL_PREFIX="http://localhost:8080"

# Server port
export PORT=8080

# CORS settings (true for permissive, false for restrictive)
export RELAX_CORS=true

# Cloudflare Turnstile secret for public API
# Use test key for local development: 1x0000000000000000000000000000000AA
# Get a real key from: https://developers.cloudflare.com/turnstile/
export TURNSTILE_SECRET="1x0000000000000000000000000000000AA"
